<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="io.charlie.app.core.modular.set.solved.mapper.ProSetSolvedMapper">

    <select id="selectSetUsers" resultType="io.charlie.app.core.modular.sys.user.entity.SysUser">
        SELECT
            su.id,
            su.group_id,
            su.username,
            su.nickname,
            su.avatar,
            su.background,
            su.quote,
            su.gender,
            su.email
        FROM
            sys_user su
        INNER JOIN
            pro_set_submit pss ON su.id = pss.user_id
        WHERE
            pss.set_id = #{setId}
        <if test="ew != null and ew.sqlSegment != null">
            ${ew.sqlSegment}
        </if>
        ORDER BY pss.create_time DESC
    </select>
</mapper>
